<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CyberQuest Academy - Course List</title>
    <style>
        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', system-ui, sans-serif;
        }

        body, html {
            background-color: #0B121F;
            color: #A4B1CD;
            min-height: 100vh;
        }

        /* Header Styles */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding: 1rem;
            background: rgba(16, 26, 47, 0.2);
            border-radius: 1rem;
            border: 1px solid rgba(83, 209, 96, 0.1);
        }

        .logo {
            font-size: 2rem;
            font-weight: bold;
            background: linear-gradient(to right, #53D160, #A7F3D0);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .search-bar {
            position: relative;
            width: 300px;
        }

        .search-bar input {
            width: 100%;
            padding: 0.75rem 1rem 0.75rem 2.5rem;
            border-radius: 9999px;
            background: #1A2333;
            border: 1px solid rgba(83, 209, 96, 0.3);
            color: #A4B1CD;
            outline: none;
            transition: all 0.3s ease;
        }

        .search-bar input:focus {
            border-color: #53D160;
            box-shadow: 0 0 10px rgba(83, 209, 96, 0.2);
        }

        /* Categories */
        .categories {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
            overflow-x: auto;
            padding-bottom: 1rem;
        }

        .category-btn {
            padding: 0.75rem 1.5rem;
            border-radius: 9999px;
            background: #1A2333;
            border: 1px solid rgba(83, 209, 96, 0.3);
            color: #A4B1CD;
            cursor: pointer;
            transition: all 0.3s ease;
            white-space: nowrap;
        }

        .category-btn:hover,
        .category-btn.active {
            background: rgba(83, 209, 96, 0.2);
            border-color: #53D160;
            color: #53D160;
        }

        /* Course Grid */
        .course-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
        }

        .course-card {
            background: #1A2333;
            border-radius: 1rem;
            border: 1px solid rgba(83, 209, 96, 0.3);
            overflow: hidden;
            transition: all 0.3s ease;
            position: relative;
        }

        .course-card:hover {
            transform: translateY(-5px);
            border-color: rgba(83, 209, 96, 0.6);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .course-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .progress-bar {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: rgba(83, 209, 96, 0.1);
        }

        .progress-fill {
            height: 100%;
            background: #53D160;
            transition: width 0.3s ease;
        }

        .course-content {
            padding: 1.5rem;
        }

        .course-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 1rem;
        }

        .course-title {
            font-size: 1.25rem;
            font-weight: bold;
            color: #53D160;
            margin-bottom: 0.5rem;
        }

        .course-rating {
            background: rgba(83, 209, 96, 0.2);
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            color: #53D160;
            font-weight: bold;
        }

        .course-description {
            color: #8291B0;
            margin-bottom: 1rem;
            font-size: 0.875rem;
        }

        .course-meta {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1rem;
            color: #8291B0;
            font-size: 0.875rem;
        }

        .course-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .level-badge {
            background: rgba(83, 209, 96, 0.1);
            color: #53D160;
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.875rem;
        }

        .start-btn {
            color: #53D160;
            background: none;
            border: none;
            cursor: pointer;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: color 0.3s ease;
        }

        .start-btn:hover {
            color: #A7F3D0;
        }

        /* Achievement Button */
        .achievement-btn {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: #53D160;
            color: #0B121F;
            width: 3.5rem;
            height: 3.5rem;
            border-radius: 9999px;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            box-shadow: 0 4px 12px rgba(83, 209, 96, 0.3);
        }

        .achievement-btn:hover {
            background: #A7F3D0;
            transform: scale(1.1);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                gap: 1rem;
            }

            .search-bar {
                width: 100%;
            }

            .categories {
                padding-bottom: 1rem;
            }

            .course-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <%- include('cursor.ejs') %>
    <%- include('navBar.ejs') %>
    <header class="header">
        <div class="logo">
            🛡️ CyberQuest Academy
        </div>
        <div class="search-bar">
            <input type="text" placeholder="Search courses...">
        </div>
    </header>

    <div class="categories">
        <button class="category-btn active">🌐 All Courses</button>
        <button class="category-btn">🔒 Network Security</button>
        <button class="category-btn">🕵️ Ethical Hacking</button>
        <button class="category-btn">🛡️ Cybersecurity Fundamentals</button>
    </div>

    <div class="course-grid" id="courseGrid">
        <!-- Courses will be dynamically added here -->
    </div>

    <button class="achievement-btn">🏆</button>

    <script>
    document.addEventListener('DOMContentLoaded', async () => {
      try {
        const response = await fetch('/api/courses');
        const courses = await response.json();
        
        const courseGrid = document.getElementById('courseGrid');
        courseGrid.innerHTML = ''; // Clear existing content
        
        courses.forEach(course => {
          const moduleCount = course.modules.length;
          const courseCard = `
            <div class="course-card">
              <img src="${course.background_image}" alt="${course.title}" class="course-image">
              <div class="progress-bar">
                <div class="progress-fill" style="width: 0%"></div>
              </div>
              <div class="course-content">
                <div class="course-header">
                  <div>
                    <h3 class="course-title">${course.title}</h3>
                    <p class="course-description">${moduleCount} module${moduleCount !== 1 ? 's' : ''}</p>
                  </div>
                  <span class="course-rating">New</span>
                </div>
                <div class="course-meta">
                  <div>⏰ ${moduleCount * 2} hours</div>
                  <div>👥 0 students</div>
                </div>
                <div class="course-footer">
                  <span class="level-badge">Beginner</span>
                  <button class="start-btn">Start Learning →</button>
                </div>
              </div>
            </div>
          `;
          courseGrid.innerHTML += courseCard;
        });
        
      } catch (error) {
        console.error('Error fetching courses:', error);
      }
    });
    </script>
</body>
</html>
